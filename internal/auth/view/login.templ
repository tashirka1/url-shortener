package view

import "url_shortener/internal/core/view"

templ LoginError(err string) {
	<div>{ err }</div>
}

templ LoginForm() {
	<form hx-post="/auth/login">
		<input type="email" name="email" placeholder="Email" aria-label="email" required/>
		<input type="password" name="password" placeholder="Пароль" aria-label="password" required/>
		<button>Войти</button>
		<div id="errors" style="margin-top:5px;color: red"></div>
	</form>
}

templ Login(userId int) {
	@view.Base("URL shortener", userId) {
		<div class="grid">
			<article style="margin: auto">
				<header><strong>Вход</strong></header>
				@LoginForm()
			</article>
		</div>
	}
}
